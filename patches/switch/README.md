# Switch patch layer

Switch-specific PokéRogue source patches belong here. Prefer `node/` scripts for
targeted, assertion-backed transformations and `patch/` for stable source diffs.

Milestone 1 intentionally has no PokéRogue source patch: it isolates Phaser 3.90
and the nx.js V8 runtime before the full game is introduced.
